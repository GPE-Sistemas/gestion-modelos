syntax = "proto3";

package gestion.modelos;

message ModuloAlarmasDomiciliarias {
  optional bool activo = 1;
  optional bool crear_dispositivos = 2;
  optional bool derivar_eventos = 3;
  optional bool derivar_eventos_tecnicos = 4;
  optional bool compartir_alarmas = 5;
}
